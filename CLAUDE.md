# CLAUDE.md

Mintlify docs site for the Telivy platform. `main` deploys to https://support.telivy.com,
so treat it as live.

The full authoring guide lives in `.github/DOCS_AUTHORING.md`, which is kept out of the
repo root because Mintlify publishes every root-level Markdown file as a page.

@.github/DOCS_AUTHORING.md
