version: 2

# Actions are pinned to commit SHAs, which is safe but freezes them.
# Dependabot keeps the pins current and annotates each bump with its version.
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    commit-message:
      prefix: ci
    labels:
      - ci
